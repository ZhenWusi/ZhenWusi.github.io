<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>RLHF 的三阶段流程</title>
      <link href="/2026/02/12/RLHF%20%E7%9A%84%E4%B8%89%E9%98%B6%E6%AE%B5%E6%B5%81%E7%A8%8B/"/>
      <url>/2026/02/12/RLHF%20%E7%9A%84%E4%B8%89%E9%98%B6%E6%AE%B5%E6%B5%81%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\assets\js\Meting.min.js"></script><h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><h3 id="RLHF-的三阶段流程"><a href="#RLHF-的三阶段流程" class="headerlink" title="RLHF 的三阶段流程"></a>RLHF 的三阶段流程</h3><p>RLHF（Reinforcement Learning from Human Feedback）通常包含三个主要阶段：</p><p><strong>监督微调（SFT）→ 偏好采样 + 奖励模型学习 → 强化学习优化（RL）</strong></p><p>简单可以理解为：先学&quot;怎么说话&quot;，再学&quot;什么话更好&quot;，最后逼模型多说&quot;好话&quot;</p><h4 id="阶段一：SFT（Supervised-Fine-Tuning）"><a href="#阶段一：SFT（Supervised-Fine-Tuning）" class="headerlink" title="阶段一：SFT（Supervised Fine-Tuning）"></a>阶段一：SFT（Supervised Fine-Tuning）</h4><p>RLHF 通常从对一个预训练语言模型进行监督微调开始，使用高质量的人工数据（如对话、摘要等），得到一个模型 $\pi_{SFT}$。</p><p><strong>数据形式</strong>：$(\text{prompt } x, \text{高质量回答 } y)$</p><p><strong>损失函数</strong>：标准的监督学习损失函数</p><p>$$\max_{\theta} \sum_{(x,y) \in D} \log \pi_\theta(y|x)$$</p><ul><li>$\pi_\theta$：策略模型的参数化形式</li><li>$D$：监督微调数据集</li><li>$x$：输入提示</li><li>$y$：目标回答</li></ul><p><strong>目标</strong>：让模型学会基本的对话能力和任务完成能力，即&quot;怎么说话&quot;</p><h4 id="阶段二：奖励模型（Reward-Modeling）"><a href="#阶段二：奖励模型（Reward-Modeling）" class="headerlink" title="阶段二：奖励模型（Reward Modeling）"></a>阶段二：奖励模型（Reward Modeling）</h4><p>使用 SFT 模型，对同一个 prompt $x$ 采样两个回答：</p><p>$$y_1, y_2 \sim \pi_{SFT}(y|x)$$</p><p>交给人类标注者，让他们选更好的一个：</p><p>$$y_w \succ y_l \mid x$$</p><ul><li>$y_w$：winner,preferred（赢家）</li><li>$y_l$：loser,rejected（输家）</li></ul><p><strong>潜在奖励函数假设</strong>：假设人类偏好是由一个未知的真实奖励函数 $r^*(x,y)$ 生成的。我们并不知道它，但假设它存在。</p><p><strong>偏好模型</strong>：</p><p>$$p^<em>(y_1 &gt; y_2 | x) &#x3D; \frac{\exp(r^</em>(x, y_1))}{\exp(r^<em>(x, y_1)) + \exp(r^</em>(x, y_2))} \tag{1}$$</p><p><strong>数据集形式</strong>：</p><p>$$D &#x3D; {(x^{(i)}, y_w^{(i)}, y_l^{(i)})}_{i&#x3D;1}^N$$</p><p><strong>损失函数</strong>：</p><p>$$\mathcal{L}<em>\text{R}(r</em>\phi, \mathcal{D}) &#x3D; -\mathbb{E}<em>{(x, y_w, y_l) \sim \mathcal{D}} \left[ \log \sigma (r</em>\phi(x, y_w) - r_\phi(x, y_l)) \right] \tag{2}$$</p><hr><h5 id="补充解释"><a href="#补充解释" class="headerlink" title="补充解释"></a>补充解释</h5><p>BT 模型：</p><p>Bradley-Terry 模型是一个用于<strong>成对比较</strong>的概率模型</p><p><strong>核心思想</strong>：</p><ul><li>每个对象 $i$ 有一个隐藏的&quot;实力值&quot;参数 $\lambda_i$</li><li>当两个对象 $i$ 和 $j$ 比较时，$i$ 战胜 $j$ 的概率为：</li></ul><p>$$P(i \succ j) &#x3D; \frac{\lambda_i}{\lambda_i + \lambda_j}$$</p><p><strong>在 DPO 中的应用</strong>：</p><ul><li>将奖励函数 $r(x,y)$ 看作&quot;实力值&quot;</li><li>人类偏好 $ y_w \succ y_l $ 就是&quot;比较结果&quot;</li><li>通过 BT 模型建立奖励与偏好的桥梁</li></ul><p>为什么不用「谁分数大就选谁」？</p><p>如果直接规定：$$y_1 \succ y_2 \iff r^<em>(x,y_1) &gt; r^</em>(x,y_2)$$<br>那问题是：人类判断有噪声，同一个人、同一个问题，不一定每次选同样的。所以我们不建模成确定性规则，而是：概率模型</p><p>把公式 (1) 改写一下：</p><p>$$p^<em>(y_1 \succ y_2 \mid x) &#x3D; \frac{1}{1 + e^{-(r^</em>(x,y_1) - r^*(x,y_2))}}$$</p><p>这就是熟悉的 Sigmoid 函数：</p><p>$$\sigma(z) &#x3D; \frac{1}{1 + e^{-z}}$$</p><p>于是：</p><p>$$p^<em>(y_1 \succ y_2 \mid x) &#x3D; \sigma(r^</em>(x,y_1) - r^*(x,y_2))$$</p><p>因为真实数据里人确实选择了 $ y_w $，所以该事件的概率是上式，对数似然是：</p><p>$$\log p_\theta(y_w \succ y_l \mid x) &#x3D; \log \sigma(r_\theta(x,y_w) - r_\theta(x,y_l))$$</p><p>最大化所有样本的对数似然：</p><p>$$\max_\theta \sum_{i&#x3D;1}^N \log \sigma(r_\theta(x^{(i)}, y_w^{(i)}) - r_\theta(x^{(i)}, y_l^{(i)}))$$</p><p>意思是：对每一条数据，都算一次&quot;模型认为人类会选赢家的概率的对数&quot;，然后把它们加起来，让这个总和尽可能大。对数把“连乘”变成“连加”,最优解不变(对数是单调递增函数)</p><p>等价地，最小化负对数似然（Likelihood）：</p><p>$$L_R &#x3D; -\mathbb{E}<em>{(x,y_w,y_l) \sim D} [\log \sigma(r</em>\theta(x,y_w) - r_\theta(x,y_l))]$$</p><p>这正是公式 (2)。意思是：对数据集里的样本取平均，然后对&quot;对数概率&quot;取负号，让这个值尽可能小。</p><p>对一个有限数据集 $D &#x3D; {z_1, ..., z_N}$，如果你均匀随机从中抽一个样本，那么：</p><p>$$\mathbb{E}<em>{z \sim D} [f(z)] &#x3D; \frac{1}{N} \sum</em>{i&#x3D;1}^N f(z_i)$$</p><p>👉 期望 &#x3D; 平均值</p><p>令 $f(x,y_w,y_l) &#x3D; \log \sigma(r_\theta(x,y_w) - r_\theta(x,y_l))$ 那么：</p><p>$$\mathbb{E}<em>{(x,y_w,y_l) \sim D} [f] &#x3D; \frac{1}{N} \sum</em>{i&#x3D;1}^N \log \sigma(r_\theta(x^{(i)}, y_w^{(i)}) - r_\theta(x^{(i)}, y_l^{(i)}))$$</p><p>因为 $\sum_{i&#x3D;1}^N f_i$ 和 $\frac{1}{N} \sum_{i&#x3D;1}^N f_i$ 只差一个常数 $\frac{1}{N}$。而在优化中：乘以一个正的常数，不会改变最优解的位置。也就是说：$$\arg\max_\theta \sum_{i&#x3D;1}^N f_i &#x3D; \arg\max_\theta \frac{1}{N} \sum_{i&#x3D;1}^N f_i$$</p><p>原目标：$$\max_\theta \mathbb{E}_D [\log \sigma(\cdots)]$$</p><p>等价于：$$\min_\theta -\mathbb{E}_D [\log \sigma(\cdots)]$$</p><p>于是定义：$$L_R &#x3D; -\mathbb{E}_D [\log \sigma(\cdots)]$$</p><p><strong>为什么机器学习里&quot;总是最小化&quot;？</strong></p><p>因为梯度下降（Gradient Descent）默认是 minimize loss，所以大家习惯：把&quot;想最大化的目标&quot;，写成&quot;要最小化的损失&quot;。</p><hr><h4 id="阶段三：RL-微调"><a href="#阶段三：RL-微调" class="headerlink" title="阶段三：RL 微调"></a>阶段三：RL 微调</h4><p><strong>RL 目标函数（核心）</strong>：</p><p>$$\max_{\pi_\theta} \mathbb{E}<em>{x \sim \mathcal{D}, y \sim \pi</em>\theta(y|x)} \left[ r_\phi(x, y) - \beta D_{\text{KL}} \left[ \pi_\theta(y|x) \middle| \pi_{\text{ref}}(y|x) \right] \right] \tag{3}$$</p><hr><h5 id="补充解释-1"><a href="#补充解释-1" class="headerlink" title="补充解释"></a>补充解释</h5><p>一句话解释：👉 让模型生成 奖励高的回答 👉 但不能偏离原始 SFT 模型太远</p><p>$\pi_{ref}$（即初始 SFT 模型 $\pi_{SFT}$）实践中，语言模型策略 $\pi_\theta$ 也被初始化为 $\pi_{SFT}$。</p><ul><li><strong>初始状态</strong>：$\pi_\theta &#x3D; \pi_{ref} &#x3D; \pi_{SFT}$</li><li><strong>训练目标</strong>：在保持与原始 SFT 模型接近的前提下，优化奖励</li><li><strong>KL 约束作用</strong>：确保模型不会偏离初始能力太远</li></ul><p><strong>KL 约束：</strong></p><p>KL 散度（Kullback-Leibler 散度）衡量两个概率分布之间的差异：</p><p>$$D_{\text{KL}}[P | Q] &#x3D; \sum_x P(x) \log \frac{P(x)}{Q(x)}$$</p><p>$$D_{\text{KL}}[\pi_\theta(y|x) | \pi_{SFT}(y|x)] &#x3D; \mathbb{E}<em>{y \sim \pi</em>\theta} \left[ \log \frac{\pi_\theta(y|x)}{\pi_{SFT}(y|x)} \right]$$</p><p>这表示：</p><ul><li>如果 $\pi_\theta$ 和 $\pi_{SFT}$ 相同：KL &#x3D; 0</li><li>如果 $\pi_\theta$ 偏离 $\pi_{SFT}$：KL &gt; 0</li><li>目标函数中的 $-\beta KL$ 惩罚偏离行为</li></ul><p><strong>KL 散度推导过程详解</strong></p><p><strong>KL 散度的通用定义（离散型）</strong>：</p><p>$$D_{\text{KL}}[P | Q] &#x3D; \sum_i P(i) \log \frac{P(i)}{Q(i)}$$</p><p><strong>期望的定义</strong>：<br>对于随机变量 $Y \sim P$，函数 $f(Y)$ 的期望定义为：</p><p>$$\mathbb{E}<em>{Y \sim P}[f(Y)] &#x3D; \sum</em>{y \in \mathcal{Y}} P(y) f(y)$$</p><p><strong>进行变量映射</strong></p><p>在 RLHF 的语境下，我们要计算的是两个模型策略（概率分布）之间的差异：</p><ul><li>$P$** (当前分布)**：$\pi_\theta(y|x)$ —— 模型当前正在学习的策略</li><li>$Q$** (参考分布)**：$\pi_{SFT}(y|x)$ —— 原始的 SFT 策略</li><li><strong>样本空间</strong>：$y$（模型生成的所有可能的回答序列）</li></ul><p>将这些代入通用公式：</p><p>$$D_{\text{KL}}[\pi_\theta | \pi_{SFT}] &#x3D; \sum_y \pi_\theta(y|x) \log \frac{\pi_\theta(y|x)}{\pi_{SFT}(y|x)}$$</p><p><strong>转化为期望形式</strong></p><ul><li>外层的 $\sum_y \pi_\theta(y|x)(\cdots)$ 表示我们在按照 $\pi_\theta(y|x)$ 的概率分布对后面的项进行加权平均</li><li>括号里的项 $\log \frac{\pi_\theta(y|x)}{\pi_{SFT}(y|x)}$ 就是要计算期望的对象</li></ul><p>因此，根据期望的定义 $ \sum P \cdot f &#x3D; \mathbb{E}_P[f] $，可以直接写成：</p><p>$$D_{\text{KL}}[\pi_\theta | \pi_{SFT}] &#x3D; \mathbb{E}<em>{y \sim \pi</em>\theta(y|x)} \left[ \log \frac{\pi_\theta(y|x)}{\pi_{SFT}(y|x)} \right]$$</p><p><strong>为什么实践中要写成期望形式？</strong></p><p>在深度学习和强化学习中，写成期望形式有重大的工程意义：</p><p><strong>无法全量求和</strong>：<br>对于语言模型，可能的回答 $y$ 的组合是天文数字（由词表大小和序列长度决定），我们根本无法遍历所有的 $y$ 来计算那个 $\sum$ 符号。</p><p><strong>蒙特卡洛采样（Monte Carlo Sampling）</strong>：<br>期望形式告诉我们，虽然不能遍历所有结果，但可以通过采样来近似：让模型 $\pi_\theta$ 生成（Sample）一批句子 $y$,计算这些句子的 $\log \pi_\theta(y|x) - \log \pi_{SFT}(y|x)$,取这些值的平均值，就是对 KL 散度的无偏估计</p><p><strong>对齐目标函数</strong>：<br>RL 的目标函数本身就是最大化期望奖励 $\mathbb{E}<em>{y \sim \pi</em>\theta}[r(x,y)]$。将 KL 约束也写成期望形式，就可以合并到一个大括号里：</p><p>$$\max_{\pi_\theta} \mathbb{E}<em>{y \sim \pi</em>\theta} \left[ r(x,y) - \beta \log \frac{\pi_\theta(y|x)}{\pi_{SFT}(y|x)} \right]$$</p><p>这样，模型在每一轮训练采样时，就可以同时优化奖励并计算 KL 惩罚。</p><p><strong>为什么这个目标函数不可导？</strong></p><p>语言模型是<strong>离散生成</strong>的：</p><ul><li>输出是 token 序列 $y &#x3D; (y_1, y_2, \ldots)$</li><li>采样 &#x2F; argmax 都是离散操作</li></ul><p>无法像普通监督学习那样，对 $\mathbb{E}<em>{y \sim \pi</em>\theta(y|x)}[r(x,y)]$ 直接对 $\theta$ 求梯度。</p><p>👉 所以不能用反向传播直接优化奖励<br>👉 必须用强化学习（policy gradient）</p><p>这就是为什么要用 REINFORCE &#x2F; PPO。</p><p><strong>KL 约束是怎么变成&quot;奖励&quot;的？</strong></p><p><strong>关键一步是：把公式 (3) KL 项写成 log-prob 的形式</strong></p><p>对离散策略：</p><p>$$D_{\text{KL}}(\pi_\theta | \pi_{ref}) &#x3D; \mathbb{E}<em>{y \sim \pi</em>\theta} \left[ \log \pi_\theta(y|x) - \log \pi_{ref}(y|x) \right]$$</p><p><strong>代回目标函数</strong>：</p><p>$$\mathbb{E}<em>{y \sim \pi</em>\theta} \left[ r_\phi(x, y) - \beta \left( \log \pi_\theta(y|x) - \log \pi_{ref}(y|x) \right) \right]$$</p><p><strong>于是可以定义一个新的 reward</strong>：</p><p>$$r(x, y) &#x3D; r_\phi(x, y) - \beta \left( \log \pi_\theta(y|x) - \log \pi_{ref}(y|x) \right)$$</p><p>即论文中的：</p><p>$$r(x, y) &#x3D; r_\phi(x, y) - \left( \log \pi_\theta(y|x) - \log \pi_{ref}(y|x) \right)$$</p><p>（论文里通常把 $\beta$ 省略或吸收到系数里）</p><p><strong>转换后的简化目标函数</strong></p><p>通过这个变换，原来的约束优化问题变成了无约束的奖励最大化问题：</p><p>$$\max_{\pi_\theta} \mathbb{E}<em>{x \sim \mathcal{D}, y \sim \pi</em>\theta(y|x)} \left[ r(x, y) \right]$$</p><p>其中 $r(x, y)$ 已经包含了 KL 惩罚项。</p><hr><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> transformers <span class="keyword">import</span> AutoModel</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RewardModel</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, model_name</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="variable language_">self</span>.backbone = AutoModel.from_pretrained(model_name)</span><br><span class="line">        <span class="variable language_">self</span>.value_head = nn.Linear(<span class="variable language_">self</span>.backbone.config.hidden_size, <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, input_ids, attention_mask</span>):</span><br><span class="line">        outputs = <span class="variable language_">self</span>.backbone(</span><br><span class="line">            input_ids=input_ids,</span><br><span class="line">            attention_mask=attention_mask</span><br><span class="line">        )</span><br><span class="line">        last_hidden = outputs.last_hidden_state[:, -<span class="number">1</span>, :]</span><br><span class="line">        reward = <span class="variable language_">self</span>.value_head(last_hidden)</span><br><span class="line">        <span class="keyword">return</span> reward</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> LLM </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
